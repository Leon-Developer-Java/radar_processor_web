# 雷达图像处理 Web 后端依赖
# Web 框架
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
python-multipart==0.0.20

# 科学计算 / 图像处理
numpy==1.26.4
scipy==1.16.1
Pillow==10.4.0
opencv-python==4.10.0.84
matplotlib==3.9.2

# YOLO 目标检测（torch 按平台单独安装，见 README）
ultralytics==8.3.203

# 文档处理
python-docx==1.2.0

# 说明：
# 1. torch/torchvision 因 CUDA 版本差异请按 README 单独安装，不在此固定。
# 2. 无 GPU 时安装 CPU 版 torch 亦可运行（推理较慢）。
